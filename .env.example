# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/menu_db

# Authentication & Security
APP_SECRET=your-super-secret-jwt-key-min-32-characters
SALT_ROUNDS=10

# CORS Configuration
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:7891,http://localhost:3000,https://yourdomain.com

# Server Configuration
PORT=7890
NODE_ENV=development

# Security Notes:
# - APP_SECRET should be a strong, random string (min 32 characters)
# - Never commit the .env file to version control
# - Use different secrets for development and production
# - In production, set NODE_ENV=production for enhanced security
